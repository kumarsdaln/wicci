@extends('admin.layouts.app')
@section('title','Media CMS')
@section('page_title','Media CMS')
@section('content')
<div class="bg-white rounded-lg shadow-sm border p-4 mb-4"><form method="GET" class="flex gap-3"><input type="text" name="q" value="{{ $q }}" placeholder="Search media" class="flex-1 border rounded px-3 py-2"><button class="px-4 py-2 bg-slate-900 text-white rounded">Search</button><a href="{{ route('admin.media.create') }}" class="px-4 py-2 bg-emerald-600 text-white rounded">Add New</a></form></div>
<div class="bg-white rounded-lg shadow-sm border overflow-hidden"><table class="min-w-full text-sm"><thead class="bg-slate-50"><tr><th class="p-3 text-left">Title</th><th class="p-3 text-left">Type</th><th class="p-3 text-left">Status</th><th class="p-3 text-left">Published</th><th class="p-3 text-left">Actions</th></tr></thead><tbody class="divide-y">@forelse($mediaItems as $item)<tr><td class="p-3">{{ $item->title }}</td><td class="p-3">{{ ucfirst($item->media_type) }}</td><td class="p-3">{{ ucfirst($item->status) }}</td><td class="p-3">{{ optional($item->published_at)->format('d M Y') ?: '-' }}</td><td class="p-3"><div class="flex gap-2">@if($item->status==='published')<a href="{{ route('media.show', $item->slug) }}" class="px-2 py-1 bg-slate-200 rounded">View</a>@endif<a href="{{ route('admin.media.edit', $item) }}" class="px-2 py-1 bg-blue-100 text-blue-700 rounded">Edit</a><form method="POST" action="{{ route('admin.media.destroy', $item) }}" onsubmit="return confirm('Delete this media item?')">@csrf @method('DELETE')<button class="px-2 py-1 bg-rose-100 text-rose-700 rounded">Delete</button></form></div></td></tr>@empty<tr><td colspan="5" class="p-4 text-slate-500">No media records found.</td></tr>@endforelse</tbody></table></div>
<div class="mt-4">{{ $mediaItems->links() }}</div>
@endsection
